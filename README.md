# تطبيق الدردشة المتكامل - Chatt Clone

تطبيق دردشة متكامل مع ميزات متقدمة للتفاعل والتواصل بين المستخدمين، مستوحى من موقع ch1tt.com.

## الميزات الرئيسية

### 🗣️ الدردشة الحية
- دردشة حية في غرف متعددة
- إرسال واستقبال الرسائل النصية
- التمرير التلقائي للرسائل الجديدة
- عرض قائمة الغرف المتاحة

### 🎨 التخصيص الشامل
- تغيير لون الاسم والخط والخلفية والحالة
- إضافة زخرفة ورموز تعبيرية
- حالة شخصية قابلة للتعديل
- معاينة حية للتغييرات

### 🎁 نظام الهدايا
- إرسال هدايا للمستخدمين
- اختيار من مجموعة متنوعة من الرموز التعبيرية
- عرض الهدايا المستقبلة

### 👥 إدارة الغرف والمستخدمين
- إنشاء غرف دردشة جديدة
- حظر المستخدمين من الغرف
- إدارة الأعضاء والأذونات

### 🔐 المصادقة الآمنة
- تسجيل دخول آمن عبر Manus OAuth
- إدارة جلسات المستخدمين
- حماية البيانات الشخصية

## المتطلبات التقنية

- **Node.js** 18+
- **pnpm** (مدير الحزم)
- **MySQL** أو قاعدة بيانات متوافقة
- **Vercel** (للنشر)

## التثبيت والتشغيل المحلي

### 1. استنساخ المشروع
```bash
git clone <repository-url>
cd chatt_clone
```

### 2. تثبيت الحزم
```bash
pnpm install
```

### 3. إعداد متغيرات البيئة
أنشئ ملف `.env.local` وأضف المتغيرات المطلوبة:
```
DATABASE_URL=your_database_url
JWT_SECRET=your_jwt_secret
VITE_APP_ID=your_app_id
OAUTH_SERVER_URL=your_oauth_url
VITE_OAUTH_PORTAL_URL=your_portal_url
```

### 4. تشغيل خادم التطوير
```bash
pnpm dev
```

سيكون التطبيق متاحاً على `http://localhost:5173`

## بنية المشروع

```
chatt_clone/
├── client/                 # الواجهة الأمامية (React)
│   ├── src/
│   │   ├── pages/         # صفحات التطبيق
│   │   ├── components/    # المكونات القابلة لإعادة الاستخدام
│   │   ├── lib/           # المكتبات والأدوات
│   │   └── App.tsx        # التطبيق الرئيسي
│   └── public/            # الملفات الثابتة
├── server/                # الواجهة الخلفية (Express + tRPC)
│   ├── routers.ts         # إجراءات tRPC
│   ├── db.ts              # دوال قاعدة البيانات
│   └── _core/             # ملفات النظام الأساسية
├── drizzle/               # إدارة قاعدة البيانات
│   └── schema.ts          # تعريف الجداول
└── vercel.json            # إعدادات النشر على Vercel
```

## الصفحات الرئيسية

| الصفحة | المسار | الوصف |
|--------|--------|-------|
| الرئيسية | `/` | صفحة الترحيب والملاحات |
| الدردشة | `/chat` | غرفة الدردشة الرئيسية |
| الإعدادات | `/settings` | تخصيص الملف الشخصي |
| المستخدمون | `/users` | الهدايا والمستخدمين |
| الإدارة | `/management` | إدارة الغرف والمستخدمين |

## الإجراءات الرئيسية (tRPC)

### الغرف
- `rooms.list()` - الحصول على قائمة الغرف
- `rooms.getById(id)` - الحصول على غرفة محددة
- `rooms.create(data)` - إنشاء غرفة جديدة

### الرسائل
- `messages.getByRoom(roomId)` - الحصول على رسائل الغرفة
- `messages.send(data)` - إرسال رسالة

### التفضيلات
- `preferences.get()` - الحصول على تفضيلات المستخدم
- `preferences.update(data)` - تحديث التفضيلات

### الهدايا
- `gifts.send(data)` - إرسال هدية
- `gifts.getReceived()` - الحصول على الهدايا المستقبلة

### الإدارة
- `management.banUser(data)` - حظر مستخدم
- `management.isBanned(data)` - التحقق من الحظر

## النشر على Vercel

### 1. ربط المشروع بـ Vercel
```bash
vercel login
vercel link
```

### 2. إعداد متغيرات البيئة
```bash
vercel env add DATABASE_URL
vercel env add JWT_SECRET
# أضف باقي المتغيرات المطلوبة
```

### 3. النشر
```bash
vercel deploy --prod
```

أو استخدم واجهة Vercel الويب لربط المستودع مباشرة.

## الاختبار

تشغيل اختبارات Vitest:
```bash
pnpm test
```

تشغيل الاختبارات مع مراقبة التغييرات:
```bash
pnpm test:watch
```

## المساهمة

نرحب بالمساهمات! يرجى:
1. عمل fork للمشروع
2. إنشاء فرع للميزة الجديدة (`git checkout -b feature/amazing-feature`)
3. الالتزام بالتغييرات (`git commit -m 'Add amazing feature'`)
4. دفع الفرع (`git push origin feature/amazing-feature`)
5. فتح Pull Request

## الترخيص

هذا المشروع مرخص تحت MIT License.

## الدعم

للمساعدة والدعم، يرجى فتح issue في المستودع.

## الملاحظات

- تأكد من أن قاعدة البيانات تعمل قبل تشغيل التطبيق
- استخدم `pnpm db:push` لتطبيق الهجرات
- تحقق من ملف `.env.example` للمتغيرات المطلوبة

---

**تم بناء هذا التطبيق بواسطة Manus AI**
